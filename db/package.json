{
  "name": "deploy",
  "dependencies": {
    "@sap/hdi-deploy": "^5",
    "@sap/hana-client": "^2"
  },
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node node_modules/@sap/hdi-deploy/deploy.js --parameter com.sap.hana.di.table/try_fast_table_migration=true",
    "build": "npm i && npx cds build .. --for hana --production"
  }
}